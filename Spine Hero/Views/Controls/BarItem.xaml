<UserControl x:Class="SpineHero.Views.Controls.BarItem"
             x:Name="BarItemUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="30"
             ToolTip="{Binding Value, ElementName=BarItemUserControl}">
    <Grid>
        <Image HorizontalAlignment="Center" VerticalAlignment="Center" 
               Visibility="{Binding IconVisibility, ElementName=BarItemUserControl}" Source="{Binding Icon, ElementName=BarItemUserControl}">
        </Image>
    </Grid>
</UserControl>
